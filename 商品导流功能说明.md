# 商品导流功能说明

## 功能概述

本小程序的产品购买模块采用**导流模式**，即展示商品信息后，引导用户跳转到其他小程序（如一方粮川）完成购买。

## ⚠️ 微信平台限制

**重要说明：** 一方粮川等实物商品交易小程序受微信平台限制，**无法通过小程序跳转小程序**。

微信官方说明：
> 为确保小程序在实物商品交易中的用户体验，经营实物商品的小程序(即包含自营类目的商家)在未满足相关标准之前，将不开放小程序跳入或跳转至其他场景能力。

因此，我们采用**复制短链接**的方式实现导流。

## 实现方案

### 当前方案：复制短链接 ✅

**优点：**
- 不受微信平台限制
- 实现简单，维护方便
- 用户体验可接受
- 适用于所有类型的小程序

**使用方式：**
1. 用户在商品详情页点击"立即购买"
2. 系统自动复制商品的短链接到剪贴板
3. 弹窗提示："已复制购买链接，请在微信聊天窗口粘贴打开即可购买"
4. 用户在微信任意聊天窗口粘贴
5. 微信自动识别短链接，跳转到一方粮川小程序

**数据库配置：**
```javascript
{
  "name": "老陈醋2.5L",
  "price": 19.9,
  "shortLink": "#小程序://一方粮川/tgIOu3r3OMlSD2b",  // 商品短链接
  // ... 其他字段
}
```

### ~~方案二：直接跳转（不可用）~~

由于微信平台限制，实物商品小程序无法使用 `wx.navigateToMiniProgram()` 直接跳转。

## 如何添加新商品

### 1. 获取商品信息

在一方粮川小程序中：
1. 找到要添加的商品
2. 点击分享按钮
3. 复制生成的短链接（格式：`#小程序://一方粮川/xxxxx`）

### 2. 录入数据库

使用 CloudBase 控制台或 MCP 工具添加商品：

```javascript
{
  "name": "商品名称",
  "category": "粮油调味",  // 分类
  "price": 19.9,  // 现价
  "originalPrice": 29.9,  // 原价
  "description": "商品描述",
  "coverImage": "/static/goods/xxx.svg",  // 商品图片
  "shortLink": "#小程序://一方粮川/xxxxx",  // 短链接
  "sold": 1580,  // 销量
  "status": "active",  // 状态
  "isRecommend": true,  // 是否推荐
  "tags": ["热销", "特产"],  // 标签
  "tagType": "hot",  // 标签类型：hot/discount/green/plain
  "imgWidth": 400,
  "imgHeight": 400
}
```

### 3. 准备商品图片

**选项A：使用 SVG 图标（推荐）**
- 在 `/static/goods/` 目录下创建 SVG 文件
- 优点：体积小，加载快，不依赖网络

**选项B：使用云存储图片**
- 上传图片到云存储
- 使用云存储路径

## 当前商品列表

| 商品名称 | 价格 | 分类 | 短链接 | 状态 |
|---------|------|------|--------|------|
| 老陈醋2.5L | ¥19.9 | 粮油调味 | ✅ 已配置 | 已上线 |

## 后续优化建议

### 短期优化
1. **批量录入商品**：从一方粮川获取更多商品信息
2. **优化提示文案**：让用户更清楚如何操作
3. **添加使用引导**：首次使用时显示操作说明

### 长期优化
1. **联系一方粮川官方**
   - 获取 AppID，实现直接跳转
   - 获取商品数据接口，自动同步
   - 建立合作关系，可能获得推广佣金

2. **数据统计**
   - 记录用户点击次数
   - 统计导流效果
   - 为合作谈判提供数据支持

## 技术实现

### 核心代码

**good-info.js (商品详情页)**
```javascript
onBuyNow() {
  const { goods } = this.data;

  // 优先使用短链接
  if (goods.shortLink) {
    wx.setClipboardData({
      data: goods.shortLink,
      success: () => {
        wx.showModal({
          title: '购买链接已复制',
          content: '请在微信聊天窗口粘贴打开，即可购买商品',
          confirmText: '知道了',
          showCancel: false
        });
      }
    });
    return;
  }

  // 如果配置了 AppID，则直接跳转
  if (goods.targetAppId && goods.targetPath) {
    wx.navigateToMiniProgram({
      appId: goods.targetAppId,
      path: goods.targetPath
    });
    return;
  }

  // 都没有配置，提示用户
  wx.showModal({
    title: '提示',
    content: '该商品暂未配置购买链接，请联系客服'
  });
}
```

## 常见问题

**Q: 为什么不直接跳转，而是要复制链接？**
A: 因为一方粮川是实物商品交易小程序，受微信平台限制，无法���用小程序跳转小程序功能。复制链接是唯一可行的方案。

**Q: 用户体验会不会很差？**
A: 虽然多了一步粘贴操作，但微信的剪贴板功能很方便，用户粘贴后可以直接跳转，体验还是可以接受的。而且这是微信平台限制，所有类似场景都需要这样处理。

**Q: 能否绕过微信的限制？**
A: 不能。这是微信平台的强制限制，无法通过技术手段绕过。任何尝试绕过的行为都可能导致小程序被封禁。

**Q: 如果联系上一方粮川，能改善吗？**
A: 即使联系上一方粮川获得 AppID，也无法直接跳转，因为��制是微信平台层面的。除非一方粮川满足微信的相关标准，解除了跳转限制。

**Q: 如何批量添加商品？**
A: 可以使用 CloudBase MCP 工具批量插入数据，或者在控制台导入 JSON/CSV 文件。

## 相关文件

- `pages/good-info/good-info.js` - 商品详情页逻辑
- `pages/good-info/good-info.wxml` - 商品详情页模板
- `pages/good-info/good-info.wxss` - 商品详情页样式
- `pages/purchase/purchase.js` - 商品列表页逻辑
- `static/goods/` - 商品图片目录

## 更新日志

- 2026-02-11: 实现复制短链接功能，添加老陈醋商品
- 2026-02-11: 完成商品详情页开发
- 2026-02-11: 实现商品列表瀑布流布局
