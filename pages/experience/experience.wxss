/* pages/experience/experience.wxss */

/* --- 顶部导航 --- */
.custom-nav {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  background: transparent;
  z-index: 999;
}

.nav-content {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 24rpx;
  height: 100%;
}

.nav-left,
.nav-right {
  width: 80rpx;
  display: flex;
  align-items: center;
}

.nav-right {
  justify-content: flex-end;
}

.nav-icon {
  width: 48rpx;
  height: 48rpx;
}

.nav-title {
  flex: 1;
  text-align: center;
  font-size: 30rpx;
  font-weight: 500;
  color: #282828;
}

.page-container {
  min-height: 100vh;
  box-sizing: border-box;
  padding-bottom: 60rpx;
  position: relative;
  z-index: 1;
}

/* --- 网格布局 (高度精确控制) --- */
.grid-section {
  display: flex;
  padding: 0 24rpx;
  gap: 20rpx;
  margin-bottom: 30rpx;
  min-height: 500rpx; /* 确保有足够高度 */
  overflow: hidden; /* 防止子元素溢出 */
}

.grid-col {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  min-width: 0; /* 关键：允许flex子元素收缩 */
  max-width: 50%; /* 限制最大宽度 */
}

.right-col {
  /* 调试：添加背景色看看是否存在 */
  /* background: rgba(255, 0, 0, 0.1); */
}

.bento-card {
  border-radius: 24rpx;
  padding: 20rpx;
  position: relative;
  overflow: hidden;
  box-sizing: border-box;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.03);
}

/* 左侧：休闲玩乐 (300rpx) */
.card-leisure {
  background: linear-gradient(135deg, #FFE180 0%, #7FD73D 100%);
  height: 300rpx;
  display: flex;
  flex-direction: column;
}

.card-tag-hot {
  position: absolute;
  top: 0;
  left: 90rpx;
  background: linear-gradient(239deg, #FCAE41 0%, #FF8100 100%);
  color: #fff;
  font-size: 18rpx;
  padding: 4rpx 10rpx;
  border-radius: 0 0 10rpx 10rpx;
  z-index: 10;
  font-weight: bold;
}

.card-header {
  margin-bottom: 12rpx;
}

.card-title {
  font-size: 32rpx;
  font-weight: 800;
  color: #282828;
  display: block;
}

.card-sub {
  font-size: 22rpx;
  color: #558F29;
}

.leisure-imgs {
  flex: 1;
  display: flex;
  gap: 12rpx;
  min-height: 0; /* 防止flex子元素溢出 */
}

.img-placeholder {
  background: rgba(255,255,255,0.6);
  border-radius: 12rpx;
  width: 50%;
  height: 100%;
  overflow: hidden;
  flex-shrink: 0; /* 防止收缩 */
}

.img-placeholder image {
  width: 100%;
  height: 100%;
  display: block; /* 防止图片底部空隙 */
}

/* 左侧：景点游玩 (180rpx) -> 左列总高 500rpx */
.card-scenic {
  background: linear-gradient(180deg, #B9EED3 0%, #DCF7BC 100%);
  height: 180rpx;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.scenic-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
}

.action-pill {
  background: linear-gradient(270deg, #FFCB49 0%, #FF8707 100%);
  color: #fff;
  font-size: 20rpx;
  padding: 6rpx 16rpx;
  border-radius: 20rpx;
  font-weight: 700;
}

.scenic-imgs {
  display: flex;
  gap: 12rpx;
  height: 80rpx;
  min-height: 0;
}

.img-mini {
  flex: 1;
  background: rgba(255,255,255,0.6);
  border-radius: 8rpx;
  height: 100%;
  overflow: hidden;
  flex-shrink: 0;
}

.img-mini image {
  width: 100%;
  height: 100%;
  display: block;
}

/* 右侧：酒店民宿 (320rpx) */
.card-hotel {
  background: linear-gradient(180deg, #ABF0A5 0%, #B7E387 100%);
  height: 320rpx;
  display: flex;
  flex-direction: column;
}

.card-sub-blue {
  font-size: 22rpx;
  color: #298F7E;
}

.hotel-imgs {
  margin-top: 16rpx;
  display: flex;
  gap: 12rpx;
  height: 100%;
  min-height: 0;
}

.img-tall {
  flex: 1;
  background: rgba(255,255,255,0.6);
  border-radius: 12rpx;
  height: 100%;
  overflow: hidden;
  flex-shrink: 0;
}

.img-tall image {
  width: 100%;
  height: 100%;
  display: block;
}

/* 右侧：特色农家菜 (160rpx) -> 右列总高 500rpx */
.card-food {
  background: linear-gradient(135deg, #FFE180 0%, #7FD73D 100%);
  height: 160rpx;
  display: flex;
  flex-direction: column;
}

.food-title {
  font-size: 26rpx;
  font-weight: 700;
  color: #298F7E;
  margin-bottom: 12rpx;
  display: block;
}

.food-imgs {
  flex: 1;
  display: flex;
  gap: 12rpx;
  min-height: 0;
}

.img-food {
  flex: 1;
  background: #f0f0f0;
  border-radius: 12rpx;
  overflow: hidden;
  flex-shrink: 0;
  height: 100%;
}

.img-food image {
  width: 100%;
  height: 100%;
  display: block;
  object-fit: cover;
}

/* --- Tab 栏 --- */
.tab-section {
  padding: 0 24rpx;
  margin-bottom: 24rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.tab-item {
  font-size: 28rpx;
  color: #666;
  position: relative;
  font-weight: 500;
}

.tab-item.active {
  color: #000;
  font-size: 32rpx;
  font-weight: 800;
}

.tab-item.active::after {
  content: '';
  position: absolute;
  bottom: -8rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 40rpx;
  height: 5rpx;
  background: linear-gradient(90deg, #6ABD16 0%, #DFE531 100%);
  border-radius: 4rpx;
  box-shadow: 0 4rpx 4rpx rgba(121, 198, 53, 0.2);
}

/* --- 推荐卡片 (核心操作区) --- */
.feed-list {
  padding: 0 24rpx;
}

.feed-card {
  background: #FDF9F7;
  border-radius: 26rpx;
  overflow: visible;
  box-shadow: 0 2rpx 20rpx rgba(0,0,0,0.1);
  margin-bottom: 40rpx;
}

.feed-img-box {
  width: 100%;
  height: 380rpx;
  background: #eee;
  border-radius: 26rpx;
  overflow: hidden;
  position: relative;
  margin: 0;
}

/* 距离标签 */
.distance-badge {
  position: absolute;
  top: 20rpx;
  right: 20rpx;
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(10rpx);
  color: #fff;
  font-size: 24rpx;
  font-weight: 600;
  padding: 8rpx 16rpx;
  border-radius: 20rpx;
  z-index: 10;
}

/* TOP 标签 */
.top-badge {
  position: absolute;
  top: 20rpx;
  left: 20rpx;
  background: linear-gradient(135deg, #FF5713 0%, #FF8D1A 100%);
  color: #fff;
  font-size: 26rpx;
  font-weight: 500;
  padding: 8rpx 20rpx;
  border-radius: 10rpx;
  z-index: 10;
  box-shadow: 0 4rpx 12rpx rgba(255, 87, 19, 0.3);
}

/* 热门标签 */
.hot-badge {
  position: absolute;
  top: 20rpx;
  left: 20rpx;
  background: linear-gradient(135deg, #ff6b6b 0%, #ff8c42 100%);
  color: #fff;
  font-size: 24rpx;
  font-weight: 700;
  padding: 10rpx 16rpx;
  border-radius: 20rpx;
  box-shadow: 0 4rpx 12rpx rgba(255, 107, 107, 0.3);
  z-index: 10;
  display: flex;
  align-items: center;
  justify-content: center;
}

.hot-icon {
  width: 70rpx;
  height: 20rpx;
}

.feed-img {
  width: 100%;
  height: 100%;
}

/* 加载状态 */
.loading-more,
.no-more,
.empty-tip {
  text-align: center;
  padding: 40rpx 0;
  font-size: 28rpx;
  color: #999;
}

.feed-info {
  padding: 32rpx 28rpx 28rpx 28rpx;
}

.info-top {
  margin-bottom: 12rpx;
}

.feed-title {
  font-size: 32rpx;
  font-weight: 500;
  color: #3E3E3E;
}

.feed-desc {
  font-size: 24rpx;
  color: #8E8E8E;
  margin-bottom: 16rpx;
  line-height: 1.6;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* 定位行 */
.feed-loc {
  display: flex;
  align-items: center;
  margin-bottom: 30rpx;
}

.icon-location {
  width: 32rpx;
  height: 32rpx;
  margin-right: 8rpx;
}

.loc-text {
  font-size: 24rpx;
  color: #7C7C7C;
}

/* 底部操作区 */
.bottom-action-area {
  margin-top: 20rpx;
  display: flex;
  align-items: center;
  gap: 16rpx;
}

/* 全宽导航按钮 */
.btn-nav-full {
  flex: 1;
  height: 72rpx;
  background: linear-gradient(90deg, #B9F8B7 0%, #C6FB88 100%);
  color: #232323;
  font-size: 24rpx;
  font-weight: 600;
  border-radius: 26rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4rpx 12rpx rgba(46, 68, 34, 0.08);
}

/* 关键：悬浮帮助按钮 (全局固定定位) */
.float-help-bubble {
  position: fixed; /* 固定定位 */
  right: 24rpx;    /* 距离右侧边距 */
  bottom: 180rpx;  /* 距离底部的高度 (避开tabbar) */

  width: 96rpx;
  height: 96rpx;
  background: #ffffff;

  /* 超椭圆/大圆角设计 */
  border-radius: 36rpx;

  /* 弥散阴影：颜色淡，扩散半径大 */
  box-shadow: 0 8rpx 24rpx rgba(142, 217, 77, 0.25);

  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  z-index: 999; /* 确保在最上层 */
}

/* 新增代码：控制 SVG 图标的大小 */
.help-icon-svg {
  width: 44rpx;
  height: 44rpx;
  margin-bottom: 2rpx; /* 稍微拉开一点文字距离 */
}

.help-text {
  font-size: 20rpx;
  color: #82F45F;
  font-weight: 600;
  margin-top: 2rpx;
  line-height: 1;
}